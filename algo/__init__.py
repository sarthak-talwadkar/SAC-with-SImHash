"""Modern PyTorch implementation of Soft Actor-Critic and pluggable
exploration bonuses (SimHash / RND / ICM).

This package is independent of the legacy TensorFlow-1 `sac/` directory,
which is kept only as a historical reference and does not run on Python 3.10.
"""
